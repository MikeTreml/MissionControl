# Mission Control TODO

## Test Lab Follow-Up

// TODO: Return to the Test Lab after Pi agent issues are handled.

- Run the new Test Lab page from the app and validate these presets first:
  - MC typecheck
  - MC smoke
  - Skill canary prep
  - Babysitter SDK Vitest
- Add pass/fail parsing for Vitest JSON reports, especially `C:\Users\Treml\source\repos\babysitter\e2e-artifacts\test-results.json`.
- Add an "Open Vitest UI" action for `http://localhost:51204/__vitest__/` when the SDK Vitest UI preset is running.
- Add preset badges for long-running, model-required, and Docker-backed commands.
- Consider editable/custom test presets after the fixed presets prove useful.

