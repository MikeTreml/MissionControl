Re-rank crop-yield priors using the 2024 NOAA snapshot.

Pull the latest NOAA monthly composite, regenerate the prior
distribution per region, and produce a side-by-side delta vs. last
year's priors.
