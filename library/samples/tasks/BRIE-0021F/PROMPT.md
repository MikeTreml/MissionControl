Convert legal brief intake to structured JSON.

The intake form currently produces a free-text PDF that lawyers re-key
into our case database. Extract fields (matter, parties, jurisdiction,
filing date, claims, relief sought) into JSON matching schema v2.
